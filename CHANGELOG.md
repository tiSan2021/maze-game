# 更新日志 · Changelog

> 版本号遵循语义化版本（SemVer）。本游戏为纯静态客户端（桌面浏览器 / 纯键盘），无部署目标；
> "发布" = 版本化 + 测试通过的静态构建包（`dist/`）与文档。

---

## [v0.5.0] — 2026-09-09（Phase 7 发布）

> 版本说明：本版本相对 v0.4.0 是一次**实质性内容 / 功能扩展**（主线 12→24、难度上限 2→3 锁），
> 因此从 0.4.0 提升至 **0.5.0**（次版本号进位，而非补丁号）。属"新功能"范畴，非仅缺陷修复。

### 新增功能
- **主线关卡 12 → 24**：构建式生成器（含兜底）现已固化 24 关，存于 `src/content/main-levels.json`。
- **三锁难度上限**：难度上限由 2 锁提升至 **3 锁**。L21–24 为 13×13 网格、迷雾视野、`lockDepth=3`、
  3 把钥匙（颜色 0/1/2）+ 3 道门（颜色 0/1/2）。
- **难度曲线（按锁深分级）**：
  - L1–3：0 锁（教学 / 纯寻路）
  - L4–8：1 锁
  - L9–20：2 锁
  - L21–24：3 锁
- **L21–24 规格（已独立复算验证）**：存储的 `meta.lockDepth` 与运行时 `computeLockDepth` 对所有 24 关一致；**0 关不可解**。

### 修复
- **卡点绕过缺陷（正确性）**：此前自避主干的贴邻段会让门形同虚设——泛洪可绕过门直达出口，
  导致 2 锁关在未开门情况下被解出（`lockDepth` 实际为 0）。
  修复位于 `src/gen/generator.ts` 的 `findChokePoints()`：门现在被钉在**真正的卡点**上
  （移除该格后 start 无法到达 exit），泛洪不再能绕过。
  - 连带保证：双锁 / 三锁的 `lockDepth` 变为**确定性保证**（生成期强制 `ld === lockPairs`，否则换种子 / 兜底）。
  - 生成器在 `construct()` 中二次回填 `meta.lockDepth = computeLockDepth(level)`，确保声明值与运行时一致（X5）。

- **每日模式「解锁一次即永久」（回归修复）**：主线 12→24 后，v0.4.0 已全清 12 关并已解锁每日的老玩家会被
  重新锁回（`isMainAllCleared()` 遍历范围扩到 24）。修复：`src/sim/progress.ts` 新增持久化标记
  `dailyUnlockedEver` 与判定函数 `isDailyUnlocked()`（`src/state/app.ts` 门禁改用它），
  并记录 12→24 迁移播种（`LEGACY_MAIN_LEVEL_COUNT = 12`），使老玩家升级后每日分区保持解锁。
- **主菜单阶段标签清理**：`演示壳 · P5-S3-E6` → `迷宫闯关 · v0.5.0`（玩家可见文案）。

### 构建与测试状态
- `vite build`：**通过**。42 模块；产物 `dist/index.html`（1.85 kB / gzip 1.07 kB）+ `dist/assets/index-*.js`（81.57 kB / gzip 17.98 kB）。
- `vitest run`：**通过**。47 测试文件 / 227 用例，全绿。
- `tsc --noEmit`：**0 错误**。
- `tests/ci-gates/perf-budget.test.ts`（main-12 最坏情况 13×13/fog/双锁帧预算）：**绿**（均值 << 16ms 门限）。

### 已知风险 / 待办
- ~~**仓库未初始化（发布阻断项）**~~ → **已解决**：仓库已 `git init` 并写入 `.gitignore`
  （忽略 `node_modules/ dist/ tmp/ .workbuddy/ spike/`），v0.5.0 已提交并打 tag（commit `fab3424`）。
- **无部署目标**：纯静态客户端游戏，无 CDN / 主机托管；"发布"即验证通过的 `dist/` 包 + 文档。
- **三锁手感待浏览器试玩确认**：3 锁 + 迷雾的体感（节奏 / 是否过难）需真机键鼠试玩，非本轮自动化覆盖。
- **U5 字号三档无障碍选项未实现**：可访问性设置目前仅含"减少动效 / 迷雾开关 / 音效"，
  字号三档（小/中/大）尚未实现——属已知缺口，**非发布阻断项**。
- ~~**遗留演示壳命名**~~ → **已修复**：改为 `迷宫闯关 · v0.5.0`。
- **dead 占位关**：`src/state/placeholder.ts` 导出 `PLACEHOLDER_LEVEL`（`id: 'demo-placeholder'`）现未被菜单路径引用
  （主线走 `main-levels.json`），属 E6 早期限位残留，建议后续清理。

---

## [v0.6.0] — 2026-09-09（Phase 7 发布）

> 版本说明：v0.5.0 之后累积的多项玩家试玩反馈迭代（选关二维网格、难度提升、可访问性 U5、选关最佳成绩展示、列数 6→5、前 8 关重画），
> 均为功能/体验扩展且向后兼容（存档 schema 仍为 1），故从 0.5.0 提升至 **0.6.0**（次版本号进位，非补丁号）。属"新功能/体验"范畴。

### 构建与测试状态
- `vite build`：**通过**（45 模块；`dist/assets/index-*.js` 84.27 kB / gzip 18.55 kB）。
- `vitest run`：**通过**。49 测试文件 / 255 用例全绿。
- `tsc --noEmit`：**0 错误**。
- `tests/ci-gates/perf-budget.test.ts`（main-24 最坏情况 13×13/fog/三锁帧预算）：**绿**（均值约 2.3ms，预算 16ms，余量约 7×）。

### 已知风险 / 待办（承接 v0.5.0）
- **前 8 关死路分支为 1 格右侧凹口**：重画主干（梯子蛇形）解决了"L 形直路一眼看穿"的病根，但死路未达"2~4 格真岔路"初衷；迷惑性主要靠解长与蛇形提升。下一轮可选：拉长死路 / 改布局增强探索压力。
- **L8（全览单锁/38步）→ L9（迷雾双锁/44步）跨度仍存**：若试玩觉得 L9 突兀，可走"B 方案"（L6–L8 提前上雾）平滑过渡。
- **3★ 判定仍偏松**（只看同一段内回头，段间重复不计）：可考虑收紧为"全程零回头"作为直接加难杠杆，留作下一轮。
- 无部署目标：纯静态客户端，无 CDN / 主机托管；"发布"即验证通过的 `dist/` 包 + 文档 + git tag。

## [v0.6.1] — 2026-09-09（试玩反馈迭代发布）

> 版本说明：v0.6.0 之后的玩家试玩反馈累积修复，以**关卡布局去重**为主线。
> 纯内容/关卡修正，向后兼容（存档 schema 仍为 1），故为 **补丁号 0.6.1**（非次版本进位）。
> 涵盖：L4–L8 二次重画为各异单一走廊蛇形；以及 v0.6.0 之后累积的选关网格化、难度提升、U5 字号三档、选关最佳成绩等。

### 修复

#### 前 8 关布局去重（二次重画，`1fa96c2`）
- 反馈：首轮重画（梯子主干）后，第 4–8 关仍"基本长的一样"——五关同为 11×11、单锁、门都钉在 row7 咽喉、仅门列不同，宏观骨架（comb：行 2/4/6/8 固定 `#######.###` / `#.#########`）完全雷同。
- 方案：在 `scripts/build-main-levels.ts` 新增 `boustrophedon()` 生成器，按参数产出**单一自避走廊**迷宫（所有地板格都在解路径上 → D9 / 死路=0 / G3 自然满足；门 A 钉在某段唯一竖向咽喉 → `lockDepth=1`）。
- L4–L8 用 **尺寸 × 镜像方向 × 门高(doorGap) × 起终点** 区分，肉眼可辨：
  - main-4 = 11×11 右起·门底（解长 48）
  - main-5 = 11×11 左起(镜像)·门顶（48）
  - main-6 = 9×9 右起·门底（24）
  - main-7 = 13×13 右起·门底（60）
  - main-8 = 11×11 左起(镜像)·门中（48）
- 验证：`npx tsx scripts/build-main-levels.ts` 全 24 关 G1/G2/G3/G5 通过；main-4/5/8 落 mid 区间 [35,55] 过 G6（main-6/7 与 L1–3 解长偏短，L1–8 本就不要求 G6）。
- 注：首轮"前 8 关难度提升（重画主干，治本）"的梯子主干方案即被本轮取代（骨架雷同），不再单独保留。

### 选关二维网格化（`d99439d`）
- 反馈：主线 24 关后只能按 ←/→ 逐关移动，选择太慢。
- 新增 `src/ui/level-grid.ts`：纯函数 `moveSelection(current, dir, maxLevel, cols)` + `MAIN_SEL_COLS = 5` + `gridRowCount()`（无 DOM、无副作用）。
- `src/main.ts` 选关覆盖层改为 **5 列 × 5 行**（24 = 5×4 + 4，最后一行残 4，残缺在底部不夹心）：**←/→ 行内移动，↑/↓ 一次跨 5 关**。
- 每日分区仍以 `1`/`2` 数字键独立进入，未纳入网格。
- 新增 `tests/unit/level-grid.test.ts`（10 用例，含最高解锁关以下的钳制）。

### 难度提升（`38c5365`）
- 反馈：难度仍偏简单。采纳组合方案（锁深已达结构性上限，故从探索维度加难）。
- **死路分支 2~4 → 3~6**：`src/gen/generator.ts` `minBranches` 2→3、`maxBranches` 4→6（顶到 G5 上限 6；分支仍强制单接触点，不触发 D9 捷径）。
- **迷雾半径 `VISION_R` 3 → 2**：视野内格数约 49 → 约 25，探索与记忆负担显著上升。
  ADR-03 仅禁止把 R 改「大」（4/5 会突破关 9+ <8ms 预算），**缩小不在禁止范围且绘制量更省**；已在 ADR-03 补 2026-09-09 修订记录。
- 重生成 `main-levels.json`：仍 24 关，死路分支实测 **3~6**（原 2~4），锁深曲线 `0→1→2→3` 不变，解长 max=80 未破 G2，**0 关不可解**。
- 同步：CI 闸门 `vision-r-constant.test.ts`（`===3`→`===2`）、控制清单 / 架构 / UX / 美术 / GDD 等**现行**文档，以及早期规划/闸门描述中的旧值；各阶段历史复盘（如 `phase5-*-retro`）保留原貌。

### 性能基准对齐难度上限
- `tests/ci-gates/perf-budget.test.ts` 最坏情况由 `main-12`（双锁 2门2钥匙）改为 `main-24`（三锁 3门3钥匙），
  使帧预算门禁真正覆盖内容天花板一档（原为发布 QA 标记的覆盖缺口）。

### 前 8 关难度提升（重画主干，治本）
- 反馈：前 8 关太简单。诊断发现病根不在参数而在**拓扑**——L1–L3 是 `right×6,down×6`、L4–L8 是 `right×8,down×8` 的**完美 L 形直路**（解长 = 网格曼哈顿最短路），分支只是走廊侧边 1 格浅凹口，且全览无迷雾。
- 方案（用户拍板）：重画 L1–L8 的 8 张手工图（`scripts/build-main-levels.ts` 的 `L1_8`），主干改为**蜿蜒自避蛇（梯子主干）** + 真死路分支；**保留教学梯度**——L1–L3 仍 0 锁纯寻路，L4–L8 仍 1 锁（1 钥匙 `a` + 1 门 `A`，同色 1:1，门钉在卡点）。
- 效果（生成脚本实测）：解长 L1–L3 `12→22/26/26`、L4–L8 `16→38`；死路分支数 L1–L8 = `3/4/4/4/5/2/4/5`（旧 L1=0）。难度曲线断言 `dead(5)=5>dead(6)=2`、`dead(8)=5≥dead(6)=2` 仍满足。
- 验证（主理人亲自重跑，非转述）：`npx tsx scripts/build-main-levels.ts` 全 24 关 G1/G2/G3/G5 通过（L1–L3 的 G6 FAIL 属设计预期——教学小关解长不在 mid 长度带 `[35,55]`，脚本对 L1–8 本就不要求 G6）；`tsc --noEmit` 0 错误；`vitest run` 49 文件 / 255 用例全绿。
- **已知打磨点（非阻断）**：本次死路分支为右侧 1 格凸出凹口（梯子主干结构所致），未完全达到"2~4 格真岔路"的初衷；迷惑性主要靠解长与蛇形提升。若要更强探索压力，下一轮可把死路拉长或改布局。

### 选关列数 6→5（UX 微调）
- 反馈：6 列一行 24 关视觉偏挤，且 4+1+5+1+5+1 出现多次单孤儿行（截图证据）。
- `src/ui/level-grid.ts` `MAIN_SEL_COLS`：6 → 5。`src/main.ts` 选关态、键盘导航（↑/↓ 一次跨 5 关）均通过常量引用，**无需改 main.ts**。
- 布局：24 = 5×4 + 4 → 5 行；前 4 行各 5 关、最后 1 行 4 关（21–24），残缺行在底部不夹心。
- 单测：补全 `tests/unit/level-grid.test.ts`（10 → 12 用例），新增强断言"最后一行残 4"与 `gridRowCount(7,5)===2`。

### 字号三档 U5（可访问性 Standard 补齐）
- 新增设置项 `uiScale`：1（标准）/ 1.25（大）/ 1.5（特大）；设置面板按 **<kbd>T</kbd>** 循环切换并持久化。
- 实现：全局 CSS 变量 `--ui-scale`（`index.html` 定义，`src/main.ts` 写入），
  覆盖层 / HUD（`hud.ts`）/ 结算面板（`settlement.ts`）字号改为 `calc(Npx * var(--ui-scale))`。
- 向后兼容：旧存档无 `uiScale` 字段 → 回落标准档且不重置其它设置（schema 版本仍为 1）。
- 顺带清理：`index.html` 的 `<title>` 由遗留的「迷宫闯关 · 演示壳 (P5-S3-E3)」改为「迷宫闯关」。

### 选关页最佳成绩（刷分展示）· `56fe8f8`
- 需求：选关页展示每关**最佳步数 / 用时**，方便刷分。
- 新增 `src/ui/format.ts`：`formatDuration(ms)` → `mm:ss`（分钟不取模，≥1 小时累加如 `60:00`），
  作为时间格式化的**单一实现**。
- 新增 `src/ui/level-best.ts`：`formatBestLine(record)` → `"N 步 · mm:ss"`；无记录（未通关）返回空串。
- `src/render/hud.ts`：删除本地 `fmtTime`，改为 import 共享 `formatDuration`，消除两处实现漂移风险。
- `src/main.ts`：选关覆盖层每个**已通关**关卡的 chip 内追加第二行最佳成绩；未通关不显示（避免噪音）。
  chip 容器 `line-height` 1.2 → 1.35 以容纳两行。
- 数据源沿用既有 `bestOf(progress, mainLevelId(n))` → `LevelRecord.bestStarSteps` / `bestStarElapsedMs`，
  **未新增存档字段**，schema 版本仍为 1。
- 新增 `tests/unit/level-best.test.ts`（7 用例）：0ms / 7.9s / 65s / 3600s 边界、
  null 与 undefined 回落空串、有记录拼接格式。

### 验证
- `tsc --noEmit`：**0 错误**。
- `vitest run`：**49 文件 / 253 用例全绿**。
- `vite build`：**通过**（45 模块；`dist/assets/index-*.js` 83.00 kB / gzip 18.55 kB）。

### 已知风险 / 待办（承接 v0.5.0）
- **三锁 + R=2 的手感待浏览器试玩确认**：是否"耐玩而非折磨"、R=2 是否过暗，需真机判断，非自动化可覆盖。
- **3★ 判定仍偏松（未改动，留作下一轮杠杆）**：`stars.ts` 的 3★ 只看**同一段内**是否回头，**段间重复访问不计**；
  多锁关被切成多段后容错极高。可考虑收紧为「全程零回头」——主干本就是自避蛇，故 3★ 仍可达、不破 G3。
- ~~**U5 字号三档无障碍选项未实现**~~ → **已实现**（见下方「字号三档 U5」）。
- **已确认其它 Standard 项已就位**：U4 四态图例（`hud.ts`，关 9+ 显示「见过 / 走过 / 未知」）、
  U7 屏幕阅读器（HUD 走 DOM，`role="status" aria-live="polite"`）。

---

---

## [v0.4.0] — 历史基线（预制作）

- 早期预制作版本（Phase 4 预制作）。主线 12 关，难度上限 2 锁，存在卡点绕过缺陷（见 v0.5.0 修复）。
- 详情见历史提交记录（若仓库已初始化）。
