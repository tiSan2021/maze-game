# Phase 5 · Sprint 0/1 回顾（Sprint Retrospective）

> 主理人：游承峰 | 阶段：Phase 5 制作 · Sprint 0/1（E0 脚手架 + E1 域内核）
> 评审强度：lean（结论先行）
> 参与：engineering-lead（实现）→ quality-lead（QA 计划 + 冒烟验证）→ design-strategist（设计评审）→ 主理人（汇编 + 裁决）

---

## 一、冲刺结果：18 个 Story 全部交付

| 项 | 结果 |
|---|---|
| 源码 | `src/` 18 个文件（core / sim / input / util） |
| 测试 | 22 文件 / **70 用例全绿** |
| DQ3 确定性 | 全仓 `Math.random` = **0 命中** |
| 关键修复 | `countDeadEndBranches` 的 `touchPoints` 由 8 邻域纠正为 **4 邻域**（C-M2-2/3 用例从 0 修正为 1） |

### 三层验证（主理人独立复核 → QA 第二层 → 设计评审）

1. **主理人独立复核**：`npm test` = 21 文件 / 66 用例全绿（vitest v1.6.1，exit 0）；`Math.random` 全仓 0 命中；通读 `src/sim/deadends.ts` 确认 M-2 实现与 GDD⑤ §4.6.1 一致。
2. **quality-lead 第二层验证**：**补 4 个边界测试**（深度=2 上界 / 非 expectedSolution 路径星级 / 连续多滑行撤销防别名 / 地图边缘视野越界）→ 70/70 全绿；未改 `src/` 业务逻辑。
3. **design-strategist 设计评审**：8 项逐条 **PASS**，总体门禁 **PASS（E1 域内核设计忠实）**，无 E1 阻塞项。

---

## 二、质量门判定：**PASS（Sprint 0/1 签字放行）**

无 FAIL 级阻塞。域内核对 GDD①-⑥ 的语义忠实，确定性底线从第一行代码起即被 CI 守护。

---

## 三、主理人裁决（对评审隐患的处置）

| # | 隐患 | 等级 | 裁决 |
|---|---|---|---|
| **H1** | **D9 互连/捷径检测空缺** —— `countDeadEndBranches` 只数合格分支，E4 的接入点独立泛洪未实现；两分支相连会导致分量少计且**不标 D9 违例**，含隐藏捷径的生成关会**静默过 G5** | 高 | **D9 独立泛洪 = E4 硬门禁，不可延后**。G5 须"D9 通过 + 计数 ≤6"**二者同时成立**才算过；E4 验收须附"两分支相连"与"双接触点"两组对照测试 |
| **H2** | GDD① §4.7 伪代码为 2 态，实现为 6 态（4 玩家态 + `WALL` + `WALL_MEMORY`） | 低-中 | **以实现为准**（L8 裁决：渲染层吸收 `WALL_MEMORY`，不改 GDD 玩法语义）。GDD① §4.7 伪代码属示意，**E2 开工前由 design-strategist 补一行澄清注**，不改动四态模型 |
| **H3** | GDD① §4.6 散文「任意可见格」与伪代码「radiusSet∩地板」不自洽 | 低 | **取散文口径**（任意可见格），实现已如此；E2 前补注对齐 |
| **H4** | Q4 交叉断言缺用例名证据留存 | 低 | QA 已确认覆盖（`q4-cross.test.ts`），留存用例名即可，不返工 |

### QA 指出的 E4 范围缺口（非 E1 缺陷）

| 缺口 | 归属 | 处置 |
|---|---|---|
| K2 同色 1:1 配对校验未暴露 | E4 加载器 | 列入 E4 验收 |
| Q2 每日种子 30 连测 100% 过 G1-G6 | E4 生成器回归网 | 列入 E4 验收 |
| M-2 的 D9 互连独立泛洪 | E4-6 | **见 H1，硬门禁** |

---

## 四、回归闸门（CI 强制，合并阻断）

`DQ3`（src+tests 扫描 0 命中，自指防护用拼接构造）· `W4`（Level 无 source/isGenerated）· `VISION_R===3`（并禁 R=4/5 分支）· `fnv1a32-single`（`0x811c9dc5` 仅 1 处）· `W1`（段切分单一实现）· `Q4` 交叉断言 · `RunSnapshot` 不含 `visited`（类型固化 + 行为验证）。

> Bug 严重级 rubric 见 `production/qa/sprint0-qa-plan.md`（S1 合并阻断 / S2 发布阻断 / S3 应修非阻塞 / S4 跟踪）。

---

## 五、下一冲刺

**Sprint 2 = E2 渲染**，以**性能尖峰开场**（CONCERN-3 准入条件，非收尾）：
- `E2-0` 尖峰：13×13 关 9+ 连续 1000 帧 P95 绘制 <8ms，跑「三前置全开」vs「缺一对照」两组。
- 尖峰达标后才展开 `E2-1…E2-6`（tiles / draw-cell / layers / pipeline / sprites / hud）。
- **超标 → 产品级决策**，回报主理人：接受关 9+ 降帧（60→30fps）/ 缩小迷宫（13→11）/ 削减动效 / 组合。
- `E3` 应用壳随后（依赖 E1，已就绪）。
