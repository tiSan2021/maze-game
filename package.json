{
  "name": "maze-game",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "俯视角 2D 网格迷宫 · 锁钥寻路闯关（Canvas 2D + TypeScript），v0.6.0（24 关主线 / 三锁难度上限 / 选关二维网格 / 前8关难度提升 / 可访问性 U5 字号三档）",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ci": "vitest run --no-color"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
