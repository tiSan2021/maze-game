# 发布前 Go / No-Go 清单 · Launch Checklist

> 目标版本：**v0.5.0**
> 请负责人 / 用户逐条勾选。带 ⛔ 为阻断项——未解决前不应放行。
> 后续还需 quality-lead 做最终 QA 签字（Final QA Sign-off）。

---

## 构建与质量
- [ ] **构建通过**：`vite build` 成功，产物 `dist/index.html` + `dist/assets/index-*.js`（81.57 kB / gzip 17.98 kB）已生成。
- [ ] **测试门禁通过**：`vitest run` = 47 文件 / 227 用例全绿；`tsc --noEmit` = 0 错误。
- [ ] **性能门禁通过**：`tests/ci-gates/perf-budget.test.ts`（main-12 最坏情况）绿，帧均值 < 16ms。

## 版本与文档
- [ ] **版本号已变更**：`package.json` `version` = **0.5.0**（自 0.4.0），`description` 已更新为发布就绪描述。
- [ ] **更新日志就绪**：根目录 `CHANGELOG.md` 含 v0.5.0 顶栏（新增功能 / 修复 / 构建测试 / 已知风险）。

## 内容与一致性
- [ ] **本地化审计完成**：确认面向玩家字符串一致为 zh-CN（无缺失 / 无错乱）；已知无 i18n 框架、仅 zh-CN。
  - 备注：主菜单 `演示壳 · P5-S3-E6` 残留内部阶段标签，发布前建议改为正式标题。
- [ ] **进度 / 存档一致性**：`src/sim/progress.ts` `MAIN_LEVEL_COUNT = 24`，题库 `main-levels.json` 含 24 关，
  每日解锁门禁（全 24 关 ≥1★）与之自洽。

## 无障碍与体验
- [ ] **可访问性缺口已确认**：U5 字号三档**未实现**——已记录为已知缺口、非阻断；其余无障碍基线（图案填充 / 钥匙双编码 / 对比度 / 墙邻感知）就位。
- [ ] **三锁手感试玩**（建议）：L21–24（3 锁 + 迷雾）已在浏览器试玩确认体感可接受（非自动化强制项）。

## 运维与回滚
- [ ] **回滚方案已文档化**：纯静态包，保留上一版 `dist/` 即可回滚，存档 schema 兼容（v1）。

## 仓库与发布（阻断项）
- [ ] ⛔ **仓库已初始化且已打 tag**：`git init` → 提交 → `git tag v0.5.0` 已完成（需用户批准，release-ops 不执行 git 命令）。
- [ ] ⛔ **发布决策（Go / No-Go）已记录**：见下文判定。

---

## 发布判定（Release Verdict）

> 由 release-ops-lead（路远行）初判，最终以 quality-lead 签字 + 用户决策为准。

- 当前初判：**GO-WITH-KNOWN-GAPS**
- 理由（一句话）：构建/测试/性能/内容一致性全绿、版本与文档就绪，但**仓库未初始化无法打 tag 为发布阻断项**，
  且 U5 字号三档与三锁真机手感为已知缺口——在用户批准并完成 `git init`+tag 后可放行。

---

### 签字栏
| 角色 | 姓名 | 日期 | 决定 |
|------|------|------|------|
| Release Ops | 路远行 | 2026-09-09 | GO-WITH-KNOWN-GAPS（待仓库 tag 解决） |
| Quality Lead | ____ | ____ | ☐ 待签 |
| 用户 / 决策者 | ____ | ____ | ☐ 待决 |
